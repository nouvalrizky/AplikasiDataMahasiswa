<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.nouvalrz.aplikasidatamahasiswa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/iconapp"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AplikasiDataMahasiswa">
        <activity
            android:name="id.nouvalrz.aplikasidatamahasiswa.UpdateActivity"
            android:exported="true" />
        <activity
            android:name="id.nouvalrz.aplikasidatamahasiswa.HomeActivity"
            android:configChanges="orientation|screenSize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="id.nouvalrz.aplikasidatamahasiswa.DisplayDataActivity"
            android:exported="true" />
        <activity
            android:name="id.nouvalrz.aplikasidatamahasiswa.SplashScreen"
            android:exported="true" />
        <activity
            android:name="id.nouvalrz.aplikasidatamahasiswa.FormActivity"
            android:exported="true">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
    </application>

</manifest>